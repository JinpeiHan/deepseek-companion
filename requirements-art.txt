# Art pipeline only. NOT shipped in the npm package (requirements.txt is the
# runtime contract and stays PySide6-only).
#
# Install into a dedicated conda environment. The GPU-backed segmentation and
# inpainting steps need a CUDA torch build, which the system interpreter does
# not have:
#
#   conda create -y -n dsh-art python=3.11
#   conda activate dsh-art
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements-art.txt
#
# Verified on 2x RTX 4090 (47.4 GiB each), driver 580 / CUDA 13:
#   torch 2.11.0+cu128, cuda.is_available() True, device_count 2
#   onnxruntime-gpu exposes TensorrtExecutionProvider and CUDAExecutionProvider
Pillow>=10.3
numpy>=1.26
scipy>=1.11
rembg>=2.0.57
onnxruntime-gpu>=1.18
opencv-python-headless>=4.9
